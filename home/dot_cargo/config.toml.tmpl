{{- if .mirrors.enabled }}
[source.crates-io]
replace-with = "{{ .crates_io.name }}"

[source.{{ .crates_io.name }}]
registry = "{{ .crates_io.url }}"

[registries.{{ .crates_io.name }}]
index = "{{ .crates_io.url }}"

{{ end -}}
[profile.release]
codegen-units = 1
lto = "fat"

[profile.bench]
codegen-units = 1
lto = "fat"

[build]
rustflags = ["-Ctarget-cpu=native"]
