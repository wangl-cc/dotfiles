; nv is a wrapper around nvim that use nvr inside neovim and fallback to vim
; if nvim is not available
[diff]
	tool = nv
[difftool "nv"]
	cmd = nv -s -d $LOCAL $REMOTE
[merge]
	tool = nv
[mergetool "nv"]
	cmd = nv -s -d $LOCAL $BASE $REMOTE $MERGED -c 'wincmd J | wincmd ='
