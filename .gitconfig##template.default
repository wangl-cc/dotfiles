[core]
	excludesFile = ~/.gitignore.d/global
[user]
	name = Long Wang
	email = wangl.cc@outlook.com
{% if yadm.os == "Darwin" %}
	signingkey = 57F47DDA!
[commit]
	gpgsign = true
[gpg]
	program = gpg
{% endif %}
[filter "ignoreline"]
	clean = "sed '/ (git): ignore this line$/d'"
	smudge = cat
[pull]
	rebase = false
[diff]
	tool = $GIT_DIFF_TOOL
[merge]
	tool = $GIT_DIFF_TOOL
[url "git@github.com:"]
	insteadOf = "https://github.com/"

# vim:ts=4:sw=4:noet
